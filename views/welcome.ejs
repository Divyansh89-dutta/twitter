<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Twitter Login Page</title>
    <!-- Latest Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Remix Icon CDN -->
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet" />
    <style>
        body {
            background: linear-gradient(135deg, #1da1f2, #14171a);
        }

        .glass {
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.1);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .hover-scale:hover {
            transform: scale(1.05);
        }

        .icon-hover:hover {
            transform: scale(1.2);
            color: #1da1f2;
        }
    </style>
</head>

<body class="h-screen flex items-center justify-center text-white">
    <div class="glass w-full max-w-md rounded-lg p-8 text-center">
        <i class="ri-twitter-fill text-6xl text-white mb-8 icon-hover transition-transform duration-500"></i>
        <h1 class="text-3xl font-bold mb-6">See what’s happening in the world right now.</h1>
        <div class="space-y-4">
            <button
                class="flex items-center justify-center w-full py-3 bg-gradient-to-r from-blue-500 to-teal-500 text-black font-semibold rounded-full shadow-md hover:scale-105 transform transition-transform duration-500">
                <i class="ri-google-fill text-xl mr-3"></i> Continue with Google
            </button>
            <button
                class="flex items-center justify-center w-full py-3 bg-gradient-to-r from-gray-800 to-gray-600 text-white font-semibold rounded-full shadow-md hover:scale-105 transform transition-transform duration-500">
                <i class="ri-apple-fill text-xl mr-3"></i> Continue with Apple
            </button>
        </div>
        <div class="my-4 text-gray-400">or</div>
        <a href="/register"
            class="block py-3 bg-gradient-to-r from-gray-700 to-gray-900 text-white font-semibold rounded-full shadow-md hover:scale-105 transform transition-transform duration-500">
            Create account
        </a>
        <p class="text-sm text-gray-400 mt-6">
            By signing up, you agree to our <a href="#" class="text-blue-400 hover:underline">Terms</a>,
            <a href="#" class="text-blue-400 hover:underline">Privacy Policy</a>, and
            <a href="#" class="text-blue-400 hover:underline">Cookie Use</a>.
        </p>
        <div class="mt-6 text-sm">
            <span>Have an account already? </span>
            <a href="/login" class="text-blue-400 hover:underline">Log in</a>
        </div>
    </div>
</body>

</html>
